# No Hardware Acceleration (Software Rendering fallback)
# Used when no GPU driver is detected

export GALLIUM_DRIVER=llvmpipe
export LIBGL_ALWAYS_SOFTWARE=1
export MESA_NO_ERROR=1
export LP_NUM_THREADS=4
# Note: Very slow — only for testing
